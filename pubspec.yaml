name: account_ledger_windows
description: Account Ledger Windows Desktop App.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0-417.4.beta <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  motion_toast: ^2.6.8
  await_sleep: ^1.0.1
  account_ledger_library_dart:
    path: account_ledger_library_dart
  integer: ^2.0.1
  audio_in_app: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/button.wav
